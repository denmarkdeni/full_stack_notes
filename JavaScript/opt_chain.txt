Optional Chaining ( ?. )

used for Accessing the nested properties in the object
without getting error.

const job = {
    person : {
        name : "kavi",
        age:22
    },
    role : "developer",
    salary : "5 LPA",
    experience : 5 
}

console.log(job.life?.name);

