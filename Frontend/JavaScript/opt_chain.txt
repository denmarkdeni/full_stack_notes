Optional Chaining ( ?. )

used for Accessing the nested properties in the object
without getting error.

const profile = {
    person : {
        name : "kavi",
        age:22
    },
    role : "developer"
}

console.log(profile.life?.name);

