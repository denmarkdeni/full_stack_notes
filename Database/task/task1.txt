MYSQL TASK 

1️⃣ Create Database 
    Create a database named: company_db
    Switch to it.

2️⃣ Create Tables

    departments
        
        | column    | datatype    | constraint       |
        | --------- | ----------- | ---------------- |
        | dept_id   | INT         | PRIMARY KEY      |
        | dept_name | VARCHAR(50) | UNIQUE, NOT NULL |

    employees

        | column    | datatype    | constraint                         |
        | --------- | ----------- | ---------------------------------- |
        | emp_id    | INT         | PRIMARY KEY                        |
        | emp_name  | VARCHAR(50) | NOT NULL                           |
        | salary    | INT         | CHECK (salary > 0)                 |
        | dept_id   | INT         | FOREIGN KEY → departments(dept_id) |
        | join_date | DATE        | NOT NULL                           | 

    projects

        | column       | datatype    | constraint  |
        | ------------ | ----------- | ----------- |
        | project_id   | INT         | PRIMARY KEY |
        | project_name | VARCHAR(50) | NOT NULL    |
        | dept_id      | INT         | FOREIGN KEY |

3️⃣ Insert Data

    Insert 3 departments

    Insert 6 employees (At least 2 employees per department)

    Insert 3 projects   

4️⃣ SELECT 

    Check all data inserted correctly.

5️⃣ WHERE + ORDER BY

    Display employees:

    salary greater than 30000

    ordered by salary descending

6️⃣ LIKE

    Find employees whose name:

    starts with A OR ends with n

7️⃣ JOIN (INNER JOIN)

    Display: employee name / department name / salary

8️⃣ GROUP BY + AGGREGATE  

    Display: department name / total employees / average salary

9️⃣ HAVING

    show only departments where average salary > 35000

🔟 JOIN + GROUP BY  

    Display: department name / number of projects assigned
